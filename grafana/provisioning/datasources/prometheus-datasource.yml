apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orglId: 1
    url: http://prometheus:9090
    basicAuth: false
    isDefault: true
    jsonData:
      timeInterval: 15s
